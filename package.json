{
  "name": "urlforge",
  "private": true,
  "description": "Tests for urlforge. The page itself has no dependencies; these are for CI only.",
  "type": "module",
  "scripts": {
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
